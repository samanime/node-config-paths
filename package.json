{
  "name": "config-paths",
  "version": "0.0.1",
  "description": "Small module for loading configuration files and integrating them with objects.",
  "repository": {
    "type": "git",
    "url": "https://github.com/samanime/node-config-paths.git"
  },
  "main": "config-paths.js",
  "devDependencies": {
    "gulp": "^3.8.11",
    "gulp-mocha": "^2.0.0"
  }
}
